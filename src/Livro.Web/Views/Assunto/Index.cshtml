@model IEnumerable<Livro.Presentation.Web.Models.AssuntoViewModel>

<partial name="_AlertMessagePartial" />
<div class="row">
    <div class="col-lg-12 table">
        <h3><label class="control-label col-md-2">Assunto</label></h3>
    </div>
</div>
<a asp-controller="Assunto" asp-action="Create" class="btn btn-primary">Adicionar</a>
<br />&nbsp;
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td>
                    <a asp-controller="Assunto" asp-action="Update" asp-route-id="@item.CodAs" class="btn btn-success">Atualizar</a>
                    <a asp-controller="Assunto" asp-action="Delete" asp-route-id="@item.CodAs" class="btn btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>